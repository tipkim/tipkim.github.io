# Tipkim Kimya Website

## Adding content

1. Create new content with the command "hugo new content <SECTIONNAME>/<FILENAME>.<FORMAT>".
2. Start the embedded web server with the command "hugo server --buildDrafts".
